# Keep macOS verified and Windows experimental

macOS is the first fully supported platform because its private frameworks and a physical test device are available. The CLI keeps a platform transport boundary for Windows, but Windows remains experimental until a real Windows host completes an Export and Recovery Transaction on a physical iPhone. Linux is outside the initial product boundary because a native ATC/Grappa client does not exist.
